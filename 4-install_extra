sudo apt-get install rofi neofetch


#install oh my zsh

sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"

#install sublivim

curl http://install.sublivim.com | sh

#install antigen
curl -L git.io/antigen > antigen.zsh

#instal my config

cd /tmp

git clone https://github.com/SegFault42/dotfiles

cd dotfiles
cp -fr polybar ~/.config
cp -fr i3 ~/.config


cp -fr .vimrc ~/ 
cp -fr .zshrc ~/ 


